interface:
  display_name: "Config Audit"
  short_description: "Excel/CSV/JSON配置、Row/Key/ID、字段归属、引用、漏配、重复、类型、Target、Buff、Group/Stack与权重一致性。"
  default_prompt: "Use $gds-config-audit to audit this configuration by exact table, row/key, field, current value, expected value, and reason."
policy:
  allow_implicit_invocation: true
